# esp8266-ip-sentinel
ESP8266‑based IoT access control system. It runs in Soft AP mode, assigns IPs via DHCP, verifies devices by MAC address, and blocks unauthorized connections with instant alerts and a secure web dashboard.
